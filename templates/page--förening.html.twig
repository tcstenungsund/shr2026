{% extends "page.html.twig" %}

{% block head %}
{% endblock %}

{% block content %}
  <div class="interior-page interior-page--forening">
    <div class="background-image-region section-hero">
      {% if page.forening_background_image %}
        <div class="background-image-media">
          {{ page.forening_background_image }}
        </div>
      {% endif %}

      {% if page.small_logo %}
        <div class="small-logo-region">
          {{ page.small_logo }}
        </div>
      {% endif %}

      <h1 class="section-hero-title">Förening</h1>
    </div>

    {% if page.navbar_region %}
      <div class="navbar-region">
        <div class="frontpage-navbar-inner">
          {{ page.navbar_region }}
        </div>
      </div>
    {% endif %}

    <div class="interior-layout interior-layout--forening">
      <a href="#main-content" id="main-content" tabindex="-1"></a>

      {% if page.forening_sidebar_1 or page.content or page.forening_content_1 %}
        <div class="interior-row interior-row--media-left">
          <aside class="interior-region interior-region--side">
            {{ page.forening_sidebar_1 }}
          </aside>
          <main class="interior-region interior-region--main">
            {{ page.content }}
            {{ page.forening_content_1 }}
          </main>
        </div>
      {% endif %}

      {% if page.forening_content_2 or page.forening_sidebar_2 %}
        <div class="interior-row interior-row--media-right">
          <section class="interior-region interior-region--main">
            {{ page.forening_content_2 }}
          </section>
          <aside class="interior-region interior-region--side">
            {{ page.forening_sidebar_2 }}
          </aside>
        </div>
      {% endif %}

      {% if page.forening_content_3 %}
        <section class="interior-region interior-region--full">
          {{ page.forening_content_3 }}
        </section>
      {% endif %}
    </div>
  </div>
{% endblock %}
